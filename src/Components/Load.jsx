import { CircularProgress, Box } from "@mui/material"

const Load = () => {
  return (
    <Box sx={{margin: '5rem 0', textAlign: 'center'}}><CircularProgress color="inherit"/></Box>
  )
}

export default Load